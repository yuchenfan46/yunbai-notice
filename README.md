# 服务平台 (yunbai-notice)

服务平台综合公告、用户协议、API 接口生态与在线公告。

基于 **VitePress** 强力驱动。

> **声明**：此项目依托在 Azure (Azure Static Web Apps) 运行，仅供参考使用。

## 本地开发

```bash
# 安装依赖
npm install

# 启动本地开发服务
npm run docs:dev

# 构建生产产物
npm run docs:build

# 本地预览产物
npm run docs:preview
```
